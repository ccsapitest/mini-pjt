# 프로젝트 규칙

## 기술 스택
- Python, LangChain, LangGraph
- 모델은 Amazon Bedrock (ChatBedrockConverse). 에이전트 본체·가드레일·로깅은 Claude Haiku 4.5
  (응답 속도), 평가용 LLM-as-Judge(`evaluation/judge_eval.py`)는 판정 품질을 위해 Claude
  Sonnet 4.5를 쓴다 — 리전 프로필(`us.anthropic...`)에 일시 장애가 있으면 크로스리전 프로필
  (`global.anthropic...`)로 우회한다
- Agent 생성은 langchain.agents 의 create_agent 를 쓴다
- 구조화 데이터(식당/메뉴/사용자) 조회는 fastmcp 기반 MCP 서버로 노출한다
- 오늘 날씨 조회는 외부 MCP 서버로 연동한다. 기상청 공공데이터포털 단기예보 조회서비스(getVilageFcst)를
  fastmcp로 직접 감싸서 만들며, 기본 조회 지역은 "경남 거제시 장평동"으로 고정한다
- 메뉴 특이사항 필터링과 식당-표준 메뉴 정규화는 langchain_chroma + BedrockEmbeddings(Titan Embed) 기반 RAG로 구현한다

## 폴더 구조 (제출 규약. 바꾸지 않는다)
> 과제 제출 규격(§4)을 그대로 따른다. 루트 폴더명은 `mini-pjt_{본인이름}` 형태를 기본으로 하되, 본인이름 표기는 유연하게 지정 가능하다.

mini-pjt_{본인이름}/
  src/
    agent.py       메인 에이전트 그래프 (추천 로직 + HITL 게이트)
    tools.py       도메인 도구 (왕복 이동시간 계산 등)
    mcp_server.py  식당·메뉴·사용자 조회 MCP 서버 + rank_restaurants(점수제 순위 계산)
    weather_mcp_server.py  기상청 단기예보 API 기반 날씨 MCP 서버 (기본 지역: 경남 거제시 장평동)
    retriever.py   메뉴명 정규화 검색 + 특이사항 기반 메뉴 필터링 (RAG 파이프라인)
  data/            식당·메뉴·사용자 더미 데이터 (JSON)
                   # 식당 목록: 현재 가상 데이터. 추후 배치 파일로 리스트 갱신 + 부서원 평가를 거쳐 실제 DB로 전환 예정
                   # 메뉴 주의사항·영양성분: 식약처 식품영양성분 데이터베이스(K-FIND, data.go.kr) 등 공신력 있는 자료로 DB화 예정
  evaluation/
    test_queries.csv   인-아웃 세트 (§4-4 스키마 확정)
                       # 컬럼: id, category(positive/negative/edge/guardrail), input, expected_traits(세미콜론 구분),
                       #       forbidden(선택, 세미콜론 구분), expected_tools(선택, 세미콜론 구분), note(선택)
    round1_report.md   1차 자체 평가 결과
    round2_report.md   2차 자체 평가 결과 (개선 후)
  SERVICE.md       서비스 스펙 (§4-3 템플릿)
  CLAUDE.md        개발 규칙 (이 문서 — 과제 제출 항목은 아니지만 계속 유지)
  README.md        제출용 설명 (§4-5 템플릿 — 설치/실행 방법 등, CLAUDE.md와 역할 분리)
  requirements.txt
  Dockerfile       (선택)
  run.sh           (선택)

## 주고받는 형식 (제출 규약 — API 표준 규약, 변경 금지)
- `POST /query`, `Content-Type: application/json`, Body: `{"question": "사용자 질의"}`
- 응답은 `answer`, `contexts`, `trace` 세 키로 고정한다
  - `answer`: 근거 기반 최종 응답 문장 (추천 식당·메뉴 안내)
  - `contexts`: 판단에 사용한 근거 문서 목록, 각 항목은 `{"doc_id": "...", "text": "..."}`
    - 식당/메뉴 조회 결과, RAG로 검색된 특이사항·정규화 매칭 근거를 이 형식으로 담는다 (예: `doc_id: "restaurant:오거리김밥"`, `doc_id: "menu:돈까스"`)
  - `trace`: 처리 단계 기록, 각 항목은 `{"step": "...", "input": "...", "output": "..."}`
    - 예: 인가 확인, 제약 병합, 특이사항 RAG 필터링, 메뉴명 정규화, 이동시간 계산 등 각 단계를 순서대로 기록

## 코드 규칙
- 파일 하나에 한 가지 역할만 둔다
- 함수와 도구에는 한국어 docstring 을 쓴다
- 비밀 값은 .env 에서 읽고 코드에 적지 않는다
- data/ 의 JSON 필드명은 임의로 바꾸지 않는다 (evaluation/test_queries.csv 와의 정합성 유지)
- 식당 추천은 **하드 필터 + 점수제**로 결정한다 (`rank_restaurants` 도구, `src/mcp_server.py`):
  - **하드 필터** (이 두 개만 후보를 통째로 배제한다): 왕복 이동시간 상한(일행이 2인 이상이면
    값이 있는 사람들 중 min(), 없는 사람은 제외), 건강 특이사항(RAG로 찾은 주의 메뉴는 안전
    문제이므로 후보 메뉴에서 하드 제외).
  - **점수제** (하드 필터를 통과한 후보들 사이의 순위만 결정, 후보 자체를 배제하지 않는다):
    일행 각자(요청자 본인 포함)마다, 그 식당에 preferred_menus와 일치하는 표준 메뉴가 있으면
    +10점, avoid_menus와 일치하면 -10점, last_menu(어제 먹은 메뉴)와 일치하면 -10점 — 사람당
    각 항목은 한 번씩만 반영한다(같은 카테고리 메뉴가 여러 개 있어도 중복 가점/감점하지
    않는다). 거리는 도보 이동일 때만 편도 5분마다 -5점(대중교통/차량은 감점 없음). 상위 3곳
    (사용자가 다른 개수를 요청하면 그 값)을 점수 내림차순으로 추천한다.
  - 사용자의 제약 항목이 비어있으면(값 없음) 그 사람은 해당 항목에 기여하지 않는다 (예: 내
    왕복 이동시간 상한이 20분이고 동행자가 값이 없으면, 하드 필터는 20분으로만 판단한다)
- 동행자 중 "사장님"이 포함되면: 하드 필터(이동시간 상한, 건강 특이사항)는 사장님 기준으로만
  정해진다(다른 일행 것은 무시). 반면 점수 계산에는 다른 일행도 그대로 참여하되, 사장님의
  가중치만 20(다른 사람은 1)으로 준다 — 사장님의 선호/회피/최근메뉴가 ±10×20으로 압도적으로
  반영되어 사실상 사장님이 원하는 방향으로 결정되지만, 극단적으로 먼 식당(거리 페널티가 200점을
  넘는 경우)까지 이길 수는 없다는 걸 알고 있어야 한다
- `rank_restaurants`는 이름만 받으면 사람별 선호·회피·최근메뉴·사장님 가중치를 내부에서 직접
  조회해서 계산하고 그 원본 데이터를 호출한 쪽(에이전트/모델)에 노출하지 않는다 — 모델이 그
  원본 데이터를 다시 읽고 "이 사람 조건과 충돌하는 것 같다"고 스스로 재해석해 진행을 멈추는
  문제가 있었기 때문에(프롬프트로 여러 번 막으려 했으나 실패), 애초에 그 판단 재료 자체를
  안 보이게 하는 쪽으로 바꿨다
- users.json 의 last_menu(어제 먹은 메뉴) 항목은 avoid_menus와 마찬가지로 이제 하드 제외가
  아니라 점수 감점(-10) 대상이다 (이전 버전에서는 하드 제외였으나, 점수제 도입 후 변경됨)
- users.json 의 나트륨 수치(max_sodium_mg) 필드는 쓰지 않는다. 대신 사용자별 특이사항(자유 텍스트, 예: "고나트륨 주의", "당뇨", "날것 주의")을 질의로 표준 메뉴(menus.json)의 주의사항을 RAG로 검색해, 해당되는 메뉴를 후보에서 제외한다 (개인별 수치 정밀도보다 자유 텍스트 매칭을 우선한다는 의도적 트레이드오프)
- 식당이 실제로 쓰는 메뉴명은 표준 메뉴(menus.json)와 다를 수 있다 (예: "이화원의 특미간짜장"과 "미자반점의 간짜장"은 동일 메뉴). 이 매칭은 RAG 유사도 검색으로 정규화하며, 유사도가 낮아 매칭이 애매하면 별칭 테이블을 만들어 보완하지 않고 해당 메뉴를 후보에서 보수적으로 제외한다 (오매칭으로 건강 주의사항이 누락되는 사고를 막는 것이 메뉴 후보를 넓히는 것보다 우선)
- 요청자 본인은 세션/thread_id(또는 별도 user_id 파라미터) 기준으로 고정한다. 이번 범위에서는 실제 로그인/인증 계층 없이 테스트·데모용 고정값을 사용한다
- 동행자는 대화에서 이름으로 명시된 사람만 인정하고, users.json 에 존재하는 사람인지 확인한다. 존재하지 않으면 "동행자를 인식할 수 없습니다. 다시 지정해주시겠어요, 아니면 혼자 드실 건가요?"라고 되묻는다
- 동행자로 지목되면 별도 동의 확인 없이 이름이 일치하는 것만으로 그 사람의 선호·제약 정보를 조회한다 (동의 절차는 이번 범위 밖). 요청자 본인과 이렇게 확인된 동행자 외의 사용자 정보는 조회하지 않는다
- 사용자 선호도·제약 수정은 사람 확인(HITL) 없이 실행하지 않는다
- Agent는 사람이 승인해도 실제 사용자 DB(users.json 파일)를 직접 수정하지 않는다. 실제 DB
  반영은 이 프로젝트 범위 밖의 별도 DB 관리 솔루션이 담당한다고 가정한다. update_user_preference는
  현재 실행 중인 MCP 서버 프로세스의 메모리 상 값만 바꿔 이번 세션 동안의 추천에만 임시로
  반영하며, 세션(프로세스)이 끝나면 원래 값으로 되돌아간다. "OO 조건 빼고 추천해줘"처럼
  이번 추천 1건에만 한정된 완화 요청은 이 도구조차 호출하지 않고, 그 턴의 계산에서만 반영한다
- 어떠한 경우에도 사용자를 새로 추가하거나 삭제할 수 없다. update_user_preference는 이미
  등록된 사용자의 필드 값만 (세션 동안 임시로) 수정하며, 등록되지 않은 이름에는 실패로
  응답한다 (신규 생성 안 함)
- 조건에 맞는 식당이 없으면 임의로 조건을 무시해 답하지 않는다. 어떤 제약 때문에 후보가 없는지 밝히고, 완화할 제약을 사용자에게 되물어 재추천한다 (HITL 아님, 일반 멀티턴 대화로 처리)
- evaluation 실행 시에는 외부 날씨 MCP를 실제로 호출하지 않고, 같은 인터페이스로 고정값을 반환하는 스텁으로 교체한다 (재현 가능한 평가를 위한 의존성 주입)
- 왕복 이동시간은 날씨 배율표를 적용해 계산한다 (이동수단 구분 없이 일괄 적용, 에이전트/평가가 같은 표를 참조한다)
  - 맑음: ×1.0
  - 그 외 모든 날씨(비, 눈, 흐림 등): ×1.2
- 식당은 건강 특이사항으로 제외하고 남은 표준 메뉴가 1개 이상 있어야 추천 후보로 인정한다 (개인
  선호/회피는 더 이상 후보 자격 조건이 아니라 순위를 정하는 점수일 뿐이다)
- 추천 응답에는 그 식당의 (건강 특이사항 제외 후 남은) 표준 메뉴를 모두 나열한다 (대표 메뉴
  1개만 고르지 않는다) — avoid_menus/last_menu에 해당하는 메뉴라고 목록에서 빼지 않는다,
  그건 이미 점수에 반영됐다
- 최종 추천 식당 개수는 기본 최대 3곳이다 (사용자가 질문에서 다른 개수를 명시하면 그 숫자를
  따른다). 정렬 기준은 항상 점수 내림차순이다 (선호·회피·최근메뉴·거리가 이미 점수 하나에
  다 반영되므로, "선호도 순"과 "이동시간 순"을 별도로 구분하지 않는다)

## 하지 말 것
- 요청하지 않은 파일을 새로 만들지 않는다
- 기존 파일을 통째로 다시 쓰지 않는다. 바뀐 부분만 고친다
